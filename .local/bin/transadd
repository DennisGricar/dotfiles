#/bin/sh

pgrep -x transmission-da >/dev/null || (transmission-daemon && notify-send "Starting transmission damoen")

transmission-remote -a "$@" && notify-send "v Transmission-daemon" "Torrent added."
